DATABASE_URL="postgresql://yallpos:yallpos123@localhost:5435/yallpos?schema=public"
JWT_SECRET="change-me-in-production-use-long-random-string"
JWT_EXPIRES_IN="8h"

# Fiscal DIAN
# simulacion | habilitacion | produccion
# Piloto activo: Restaurante de Yall (NIT 290329032903)
# Ver docs/PILOTO-RESTAURANTE-YALL.md

FISCAL_ENV="simulacion"
FISCAL_SOFTWARE_ID="YALLPOS-001"
FISCAL_SOFTWARE_PIN="pin-habilitacion"
FISCAL_CERT_PATH="./certs/certificado.p12"
FISCAL_CERT_PASSWORD="password-del-certificado"
FISCAL_TEST_SET_ID=""

# Notificaciones reservas (opcional, por sucursal en Configuración web)
# RESERVATION_WEBHOOK_URL se configura en UI → Configuración → Notificaciones

# Impresión — caja (tiquetes, reporte X)
PRINTER_IP="192.168.1.100"
PRINTER_PORT=9100
# Cocina (comandas) — si no se define, usa la misma impresora de caja
KITCHEN_PRINTER_IP="192.168.1.101"
KITCHEN_PRINTER_PORT=9100
PRINT_AGENT_PORT=9101

PORT=3000
